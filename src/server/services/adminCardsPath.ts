/** SCR-18 — the administration list of every flashcard; also where an administrator returns to after SCR-12 or SCR-09 removes a card. */
export const ADMIN_CARDS_PATH = "/administracja/fiszki";

/** The first crumb of SCR-12 for an administrator. */
export const ADMIN_CARDS_LABEL = "Wszystkie fiszki";
